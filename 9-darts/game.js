const WS_URLS=["ws://127.0.0.1:3180/api/events?type=state","ws://localhost:3180/api/events?type=state"];
const ORDER=[20,1,18,4,13,6,10,15,2,17,3,19,7,16,8,11,14,9,12,5];
const COLORS={black:"#11131a",cream:"#eee9d4",red:"#f15b62",green:"#61b86f"};
const state={player:"",darts:0,score:0,singles:0,doubles:0,triples:0,bulls:0,misses:0,running:false,connected:false,socket:null,urlIndex:0,latestNumThrows:null,baselineThrows:null,lastSignature:null,audio:null,history:loadHistory(),throws:[],sectorHits:{},currentStreak:0,bestStreak:0,coordsExact:0};
const $=id=>document.getElementById(id);
function polar(cx,cy,r,a){a=(a-90)*Math.PI/180;return[cx+r*Math.cos(a),cy+r*Math.sin(a)]}
function wedge(cx,cy,r1,r2,a1,a2){const p1=polar(cx,cy,r2,a1),p2=polar(cx,cy,r2,a2),p3=polar(cx,cy,r1,a2),p4=polar(cx,cy,r1,a1),large=a2-a1>180?1:0;return`M ${p1[0]} ${p1[1]} A ${r2} ${r2} 0 ${large} 1 ${p2[0]} ${p2[1]} L ${p3[0]} ${p3[1]} A ${r1} ${r1} 0 ${large} 0 ${p4[0]} ${p4[1]} Z`}
function buildBoard(){const svg=$("boardSvg"),ns="http://www.w3.org/2000/svg",cx=250,cy=250;svg.innerHTML="";const R={o:220,d:188,to:126,ti:110,bo:38,bi:17};const add=(num,bed,d,fill)=>{const p=document.createElementNS(ns,"path");p.setAttribute("d",d);p.setAttribute("fill",fill);p.setAttribute("stroke","#10131b");p.setAttribute("stroke-width","1.5");p.classList.add("board-segment");svg.appendChild(p)};const bg=document.createElementNS(ns,"circle");bg.setAttribute("cx",cx);bg.setAttribute("cy",cy);bg.setAttribute("r",230);bg.setAttribute("fill","#070a12");bg.setAttribute("stroke","#34405f");bg.setAttribute("stroke-width",2);svg.appendChild(bg);ORDER.forEach((num,i)=>{const a1=i*18-8.3,a2=i*18+8.3,red=i%2;add(num,"single",wedge(cx,cy,R.ti,R.d,a1,a2),red?COLORS.red:COLORS.cream);add(num,"outer",wedge(cx,cy,R.d,R.o,a1,a2),red?COLORS.red:COLORS.green);add(num,"triple",wedge(cx,cy,R.to,R.ti,a1,a2),red?COLORS.red:COLORS.green);add(num,"inner",wedge(cx,cy,R.bo,R.to,a1,a2),red?COLORS.red:COLORS.cream);const[x,y]=polar(cx,cy,246,i*18),t=document.createElementNS(ns,"text");t.setAttribute("x",x);t.setAttribute("y",y);t.setAttribute("text-anchor","middle");t.setAttribute("dominant-baseline","middle");t.setAttribute("class","board-label");t.textContent=num;svg.appendChild(t)});[[R.bo,COLORS.green],[R.bi,COLORS.red]].forEach(([r,fill])=>{const c=document.createElementNS(ns,"circle");c.setAttribute("cx",cx);c.setAttribute("cy",cy);c.setAttribute("r",r);c.setAttribute("fill",fill);c.setAttribute("stroke","#11131a");c.setAttribute("stroke-width",2);svg.appendChild(c)});const bt=document.createElementNS(ns,"text");bt.setAttribute("x",cx);bt.setAttribute("y",cy+3);bt.setAttribute("text-anchor","middle");bt.setAttribute("class","board-label");bt.setAttribute("font-size",10);bt.textContent="BULL";svg.appendChild(bt)}
function setConnection(kind,text){$("connection").className="status "+kind;$("connection").innerHTML=`<span class="status-dot"></span><span>${text}</span>`}
function connect(){if(state.socket){try{state.socket.close()}catch(_){}}setConnection("connecting","Connexion à Autodarts…");const ws=new WebSocket(WS_URLS[state.urlIndex]);state.socket=ws;ws.onopen=()=>{state.connected=true;state.urlIndex=0;setConnection("online","Autodarts connecté");$("startBtn").disabled=false;$("setupMessage").textContent="Autodarts connecté. Prêt à jouer."};ws.onmessage=e=>{try{handleAutodarts(JSON.parse(e.data))}catch(err){console.warn(err)}};ws.onclose=()=>{state.connected=false;setConnection("offline","Autodarts non connecté");$("startBtn").disabled=true;if(!state.running)$("setupMessage").textContent="Autodarts non détecté. Vérifie que le Board Manager est ouvert.";state.urlIndex=(state.urlIndex+1)%WS_URLS.length;setTimeout(connect,2500)}}
function normalizeSegment(s){return{number:Number(s?.number??0),mult:Number(s?.multiplier??0),name:s?.name||"MISS",bed:(s?.bed||"Outside").toLowerCase()}}
function extractCoords(t){const cs=[t?.coords,t?.coordinates,t?.position,t?.point,t?.hit?.coords,t?.dart?.coords];for(const c of cs){if(c!=null){const x=Number(c.x),y=Number(c.y);if(Number.isFinite(x)&&Number.isFinite(y))return{x,y}}}const x=Number(t?.x),y=Number(t?.y);return Number.isFinite(x)&&Number.isFinite(y)?{x,y}:null}
function normalizeCoords(c){if(!c)return null;let x=Number(c.x),y=Number(c.y);if(!Number.isFinite(x)||!Number.isFinite(y))return null;const m=Math.max(Math.abs(x),Math.abs(y));if(m>1.5){x/=m;y/=m}return{x,y}}
function classify(s){const name=(s.name||"").toLowerCase(),bed=(s.bed||"").toLowerCase();if(s.number===25||name.includes("bull")){const fifty=name.includes("bullseye")||name.includes("inner")||bed.includes("inner")||s.mult===2;return{points:fifty?50:25,kind:"bull"}}if(s.number>=1&&s.number<=20&&s.mult>=1&&s.mult<=3)return{points:s.number*s.mult,kind:s.mult===3?"triple":s.mult===2?"double":"single"};return{points:0,kind:"miss"}}
function handleAutodarts(msg){if(msg?.type!=="state"||!msg.data)return;const d=msg.data;if(d.event!=="Throw detected"||!Array.isArray(d.throws)||!d.throws.length)return;const rawThrow=d.throws[d.throws.length-1],nt=Number(d.numThrows??0),s=normalizeSegment(rawThrow?.segment);const coords=normalizeCoords(extractCoords(rawThrow));const sig=`${nt}|${s.name}|${s.number}|${s.mult}|${s.bed}|${coords?`${coords.x.toFixed(5)},${coords.y.toFixed(5)}`:"none"}`;if(nt===state.latestNumThrows&&sig===state.lastSignature)return;state.latestNumThrows=nt;state.lastSignature=sig;if(!state.running||state.baselineThrows!==null&&nt<=state.baselineThrows||state.darts>=9)return;const c=classify(s);state.darts++;state.score+=c.points;if(c.kind==="single")state.singles++;else if(c.kind==="double")state.doubles++;else if(c.kind==="triple")state.triples++;else if(c.kind==="bull")state.bulls++;else state.misses++;if(c.points>=20)state.currentStreak++;else state.currentStreak=0;state.bestStreak=Math.max(state.bestStreak,state.currentStreak);if(coords)state.coordsExact++;state.sectorHits[s.number]=(state.sectorHits[s.number]||0)+1;state.throws.push({n:state.darts,name:s.name,number:s.number,mult:s.mult,bed:s.bed,points:c.points,kind:c.kind,coords});addThrow(s.name,c.points,c.kind,s.mult);playDartSound(c.kind,s.mult);updateGameUI();renderHeatmap();if(state.darts>=9)finishGame()}
function startGame(){if(!state.connected)return;state.player=(($("playerName").value||"Joueur").trim()||"Joueur").slice(0,24);localStorage.setItem("9darts.player",state.player);state.darts=state.score=state.singles=state.doubles=state.triples=state.bulls=state.misses=0;state.running=true;state.baselineThrows=state.latestNumThrows;state.lastSignature=null;state.throws=[];state.sectorHits={};state.currentStreak=0;state.bestStreak=0;state.coordsExact=0;$("gamePlayer").textContent=state.player.toUpperCase();$("throws").innerHTML="";$("setup").classList.add("hidden");$("finish").classList.add("hidden");$("game").classList.remove("hidden");updateGameUI();renderHeatmap();playStartSound()}
function addThrow(name,points,kind,mult){const row=document.createElement("div");row.className="throw "+(points>0?"good":"zero")+(kind==="triple"?" triple":"");const label=kind==="triple"?`TRIPLE · +${points}`:kind==="double"?`DOUBLE · +${points}`:kind==="bull"?`BULL · +${points}`:kind==="single"?`SIMPLE · +${points}`:"RATÉ · +0";row.innerHTML=`<div class="throw-left"><div class="throw-number">${state.darts}</div><div><div class="throw-name">${escapeHtml(name)}</div><div class="throw-detail">${label}</div></div></div><div class="throw-points">+${points}</div>`;$("throws").prepend(row);while($("throws").children.length>30)$("throws").lastChild.remove()}
function updateGameUI(){const p=Math.round(state.darts/9*100),avg=state.darts?state.score/state.darts:0,a3=state.throws.slice(-3).reduce((a,x)=>a+x.points,0)/Math.min(3,state.throws.length||1),hits=state.singles+state.doubles+state.triples+state.bulls;$("score").textContent=state.score;$("scoreCenter").textContent=state.score;$("dartCount").textContent=state.darts;$("percent").textContent=p+"%";$("progressBar").style.width=p+"%";$("average").textContent=avg.toFixed(2);$("avg3all").textContent=(avg*3).toFixed(2);$("singleCount").textContent=state.singles;$("doubleCount").textContent=state.doubles;$("tripleCount").textContent=state.triples;$("bullCount").textContent=state.bulls;$("pace9").textContent=state.score;$("lastDart").textContent=state.throws.length?formatThrow(state.throws[state.throws.length-1]):"—";$("avg3").textContent=state.throws.length?a3.toFixed(2):"0.00";$("bestDart").textContent=state.throws.length?Math.max(...state.throws.map(x=>x.points)):0;$("tripleRate").textContent=(state.darts?state.triples/state.darts*100:0).toFixed(0)+"%";$("exactRate").textContent=(state.darts?state.coordsExact/state.darts*100:0).toFixed(0)+"%";$("targetHits").textContent=state.triples;$("currentStreak").textContent=state.currentStreak;$("bestStreak").textContent=state.bestStreak}
function formatThrow(t){return t?`${t.name} · +${t.points}`:"—"}
function finishGame(){state.running=false;const r={id:Date.now(),player:state.player,score:state.score,average:state.score/9,best:Math.max(...state.throws.map(x=>x.points)),triples:state.triples,bulls:state.bulls,date:new Date().toISOString()};state.history.unshift(r);state.history=state.history.slice(0,20);saveHistory(state.history);$("finalScore").textContent=state.score;$("finishPlayer").textContent=state.player.toUpperCase();$("finalAverage").textContent=r.average.toFixed(2);$("finalBest").textContent=r.best;$("finalTriples").textContent=r.triples;$("finalBulls").textContent=r.bulls;const best=getPersonalBest(state.player);$("bestMessage").textContent=best&&best.id===r.id?"🔥 Nouveau meilleur score local !":"";$("game").classList.add("hidden");$("finish").classList.remove("hidden");renderHistory();playFinishSound()}
function resetToSetup(){state.running=false;$("finish").classList.add("hidden");$("game").classList.add("hidden");$("setup").classList.remove("hidden");$("startBtn").disabled=!state.connected;$("shareMessage").textContent=""}
function shareResult(){const text=`🎯 9 FLÉCHETTES\n${state.player}\nScore : ${state.score} points\nMoyenne : ${(state.score/9).toFixed(2)} / fléchette`;if(navigator.share){navigator.share({title:"9 Fléchettes",text,url:location.href}).catch(()=>{});return}if(navigator.clipboard)navigator.clipboard.writeText(text).then(()=>$("shareMessage").textContent="Résultat copié dans le presse-papiers !").catch(()=>fallbackCopy(text));else fallbackCopy(text)}
function fallbackCopy(text){const ta=document.createElement("textarea");ta.value=text;document.body.appendChild(ta);ta.select();try{document.execCommand("copy");$("shareMessage").textContent="Résultat copié !"}catch(_){$("shareMessage").textContent=text}ta.remove()}
function loadHistory(){try{return JSON.parse(localStorage.getItem("9darts.history")||"[]")}catch(_){return[]}}function saveHistory(h){localStorage.setItem("9darts.history",JSON.stringify(h))}function getPersonalBest(player){return state.history.filter(x=>x.player===player).sort((a,b)=>b.score-a.score)[0]||null}function renderHistory(){const b=$("history");if(!state.history.length){b.innerHTML='<div class="empty-history">Aucun résultat enregistré sur cet appareil.</div>';return}b.innerHTML=state.history.slice().sort((a,b)=>b.score-a.score).slice(0,10).map((x,i)=>`<div class="history-row"><b>${i===0?"🏆 ":""}${escapeHtml(x.player)}</b><span>${x.score} pts</span><span>${x.average.toFixed(2)} / dart</span><span>${new Date(x.date).toLocaleDateString("fr-FR")}</span></div>`).join("")}
function clearHistory(){if(!confirm("Effacer l'historique local ?"))return;state.history=[];saveHistory([]);renderHistory()}function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
function buildHeatBoard(){const svg=$("heatSvg"),ns="http://www.w3.org/2000/svg",cx=250,cy=250;svg.innerHTML="";const R={o:214,d:184,to:126,ti:108,bo:40,bi:18};const bg=document.createElementNS(ns,"circle");bg.setAttribute("cx",cx);bg.setAttribute("cy",cy);bg.setAttribute("r",226);bg.setAttribute("fill","#070a12");bg.setAttribute("stroke","#303a58");bg.setAttribute("stroke-width",2);svg.appendChild(bg);ORDER.forEach((num,i)=>{const a1=i*18-8.3,a2=i*18+8.3;[["single",R.ti,R.d],["outer",R.d,R.o],["triple",R.to,R.ti],["inner",R.bo,R.to]].forEach(([bed,r1,r2])=>{const p=document.createElementNS(ns,"path");p.setAttribute("d",wedge(cx,cy,r1,r2,a1,a2));p.dataset.number=num;p.dataset.bed=bed;p.classList.add("heat-segment");svg.appendChild(p)});const[x,y]=polar(cx,cy,240,i*18),t=document.createElementNS(ns,"text");t.setAttribute("x",x);t.setAttribute("y",y);t.setAttribute("text-anchor","middle");t.setAttribute("dominant-baseline","middle");t.classList.add("heat-label");t.textContent=num;svg.appendChild(t)});const bull=document.createElementNS(ns,"circle");bull.setAttribute("cx",cx);bull.setAttribute("cy",cy);bull.setAttribute("r",R.bo);bull.classList.add("heat-bull");bull.dataset.number="25";svg.appendChild(bull);const bullseye=document.createElementNS(ns,"circle");bullseye.setAttribute("cx",cx);bullseye.setAttribute("cy",cy);bullseye.setAttribute("r",R.bi);bullseye.classList.add("heat-bullseye");bullseye.dataset.number="25";svg.appendChild(bullseye);renderHeatmap()}
function heatAlpha(count,max){return count?Math.min(.82,.10+.68*(count/max)):0}function coordToSvg(c){return c?{x:250+c.x*205,y:250-c.y*205}:null}function dartColor(t){return t.kind==="miss"?"#8a94ad":t.kind==="triple"?"#62e6a8":t.kind==="double"?"#ff6974":t.kind==="bull"?"#ffca5c":"#dce3fa"}
function renderImpactLayer(svg,ns){const layer=document.createElementNS(ns,"g");layer.classList.add("impact-layer");state.throws.forEach(t=>{const pos=coordToSvg(t.coords);if(!pos)return;const color=dartColor(t),halo=document.createElementNS(ns,"circle");halo.setAttribute("cx",pos.x);halo.setAttribute("cy",pos.y);halo.setAttribute("r",t.kind!=="miss"?15:11);halo.setAttribute("fill",color);halo.setAttribute("fill-opacity",t.kind!=="miss"?.12:.07);halo.classList.add("impact-halo");layer.appendChild(halo);const dot=document.createElementNS(ns,"circle");dot.setAttribute("cx",pos.x);dot.setAttribute("cy",pos.y);dot.setAttribute("r",t.n===state.darts?5.2:3.6);dot.setAttribute("fill",color);dot.setAttribute("stroke","#080b12");dot.setAttribute("stroke-width",2);dot.classList.add("impact-dot");const title=document.createElementNS(ns,"title");title.textContent=`#${t.n} · ${t.name} · +${t.points} · x ${t.coords.x.toFixed(3)} · y ${t.coords.y.toFixed(3)}`;dot.appendChild(title);layer.appendChild(dot)});svg.appendChild(layer)}
function renderHeatmap(){const svg=$("heatSvg");if(!svg)return;const max=Math.max(1,...Object.values(state.sectorHits));document.querySelectorAll(".heat-segment").forEach(el=>{const n=Number(el.dataset.number),count=state.sectorHits[n]||0,alpha=heatAlpha(count,max);el.style.fill=count?`rgba(98,230,168,${alpha})`:"rgba(255,255,255,.035)";el.style.stroke="rgba(255,255,255,.07)";el.style.strokeWidth="1"});const hb=svg.querySelector(".heat-bull"),hs=svg.querySelector(".heat-bullseye");if(hb)hb.style.fill=state.sectorHits[25]?`rgba(255,202,92,${heatAlpha(state.sectorHits[25],max)})`:"rgba(255,255,255,.04)";if(hs)hs.style.fill=state.sectorHits[25]?`rgba(255,91,98,${heatAlpha(state.sectorHits[25],max)})`:"rgba(255,255,255,.04)";document.querySelectorAll(".heat-label").forEach(t=>{const n=Number(t.textContent);t.style.opacity=state.sectorHits[n] ? .95 : .42;t.style.fill="#dce3fa"});const old=svg.querySelector(".impact-layer");if(old)old.remove();renderImpactLayer(svg,"http://www.w3.org/2000/svg");$("exactHits").textContent=state.throws.filter(t=>t.coords).length;$("coordMode").textContent=state.throws.some(t=>t.coords)?"COORDONNÉES RÉELLES":"EN ATTENTE DES COORDONNÉES"}
function audioContext(){if(!state.audio)state.audio=new(window.AudioContext||window.webkitAudioContext)();if(state.audio.state==="suspended")state.audio.resume();return state.audio}function beep(freq,dur,type="sine",gain=.035,delay=0){try{const a=audioContext(),o=a.createOscillator(),g=a.createGain(),t=a.currentTime+delay;o.type=type;o.frequency.setValueAtTime(freq,t);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(gain,t+.015);g.gain.exponentialRampToValueAtTime(.0001,t+dur);o.connect(g);g.connect(a.destination);o.start(t);o.stop(t+dur+.03)}catch(_){} }function playStartSound(){beep(520,.12);beep(780,.16,"sine",.035,.09)}function playDartSound(kind,m){if(kind==="miss"){beep(180,.07,"square",.018);return}if(kind==="triple"){beep(660,.1,"sine",.045);beep(990,.14,"sine",.05,.08);beep(1320,.18,"sine",.045,.16)}else if(kind==="double"){beep(620,.13,"sine",.04);beep(820,.14,"sine",.035,.1)}else beep(520,.09,"sine",.03)}function playFinishSound(){[523,659,784,1047].forEach((f,i)=>beep(f,.16,"sine",.04,i*.11))}
$("startBtn").onclick=startGame;$("resetBtn").onclick=resetToSetup;$("againBtn").onclick=resetToSetup;$("shareBtn").onclick=shareResult;$("clearHistory").onclick=clearHistory;$("playerName").value=localStorage.getItem("9darts.player")||"";buildBoard();buildHeatBoard();renderHistory();connect();
